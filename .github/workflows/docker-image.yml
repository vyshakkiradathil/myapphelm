name: Build and Push Docker Image

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    
    - name: Build and push Docker image JFROG Artifacotry
      env:
        DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
        DOCKERHUB_PASSWORD: ${{ secrets.DOCKERHUB_PASSWORD }}
        IMAGE_TAG: ${{ github.run_id }}
      run: |
        docker login -u $DOCKERHUB_USERNAME myjfrogtestenv.jfrog.io -p $DOCKERHUB_PASSWORD
        docker build -t helloworld-app:${{ env.IMAGE_TAG }} .
        docker tag helloworld-app:${{ env.IMAGE_TAG }} myjfrogtestenv.jfrog.io/docker/helloworld-app:${{ env.IMAGE_TAG }}
        docker push myjfrogtestenv.jfrog.io/docker/helloworld-app:${{ env.IMAGE_TAG }}

    - name: Helm Package
      env:
        HELM_USERNAME: ${{ secrets.HELM_USERNAME }}
        HELM_PASSWORD: ${{ secrets.HELM_PASSWORD }}
      run: |
        helm registry login https://myjfrogtestenv.jfrog.io/artifactory/api/helm/helm-helm --username ${{ env.HELM_USERNAME }}  --password ${{ secrets.HELM_PASSWORD }}
        helm repo add helm-helm https://myjfrogtestenv.jfrog.io/artifactory/api/helm/helm-helm --username ${{ env.HELM_USERNAME }}  --password ${{ secrets.HELM_PASSWORD }}
        helm package myhelmapp
        curl -u ${{ env.HELM_USERNAME }}:${{ secrets.HELM_PASSWORD }} -T /home/runner/work/myapphelm/myapphelm/myhelmapp-0.1.5.tgz "https://myjfrogtestenv.jfrog.io/artifactory/helm-helm/myhelmapp-0.1.5.tgz"

    # helm push myhelmapp-0.1.0.tgz oci://myjfrogtestenv.jfrog.io/helm-helm
    #helm push myhelmapp-0.1.0.tgz helm-helm

    

    # - name: Build and push Docker image
    #   env:
    #     DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
    #     DOCKERHUB_PASSWORD: ${{ secrets.DOCKERHUB_PASSWORD }}
    #     IMAGE_TAG: ${{ github.run_id }}
    #   run: |
    #     docker login --username $DOCKERHUB_USERNAME --password $DOCKERHUB_PASSWORD
    #     docker build -t helloworld-app:${{ env.IMAGE_TAG }} .
    #     docker tag helloworld-app:${{ env.IMAGE_TAG }} $DOCKERHUB_USERNAME/helloworld-app:${{ env.IMAGE_TAG }}
    #     docker push $DOCKERHUB_USERNAME/helloworld-app:${{ env.IMAGE_TAG }}

    # - name: Helm Package
    #   env:
    #     HELM_USERNAME: ${{ secrets.HELM_USERNAME }}
    #     HELM_PASSWORD: ${{ secrets.HELM_PASSWORD }}
    #   run: |
    #     helm registry login ${{ env.HELM_USERNAME }}.azurecr.io --username ${{ env.HELM_USERNAME }}  --password ${{ secrets.HELM_PASSWORD }}
    #     helm package myhelmapp
    #     helm push myhelmapp-0.1.0.tgz oci://${{ env.HELM_USERNAME }}.azurecr.io/helm

    # - name: Set up Helm
    #   uses: azure/setup-helm@v1
    #   with:
    #     version: v3.7.0

    # - name: Package Helm Chart
    #   run: |
    #     helm package myhelmapp

    # - name: Publish Helm Chart to GitHub Packages
    #   run: |
    #     docker login ghcr.io -u ${{ github.repository_owner }} -p ${{ secrets.GITHUB_TOKEN }}
    #     helm push myhelmapp-0.1.0.tgz oci://ghcr.io/${{ github.repository_owner }}/myhelmapp:0.1.0
    #   env:
    #     CR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #     HELM_EXPERIMENTAL_OCI: 1

